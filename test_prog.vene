/* Muutujate deklaratsioonid */
аргумент плата = -990; аргумент имя  = "Афанасий";


/* Funktsiooni väljakutse */
вещать(имя, плата);

/* Massiivi deklaratsioon ja väljastamine */
сборище сб = ["Вася", "Федя", "Супермэн"];
перечень пе = [|1, 5, 7|];
скопище ск = [имя, плата, "Петя", 12]; 
вещать(ск);

нум = цифра(ввод("помести число"));

/* if-lause */
ежели(нум > 100) тогда {
    вещать("благодать!")
} иначе {
    вещать("бес вселился!!")
}; 

аргумент инд = 0;

/* while-tsükkel */
доколе(инд < нум) {
    ежели (инд > 3) тогда {
		вещать(инд)
	}иначе{
		пущай;
	};
    инд = инд + 1
};

/* for-tsükkel */
покаместъ(аргумент йо = 0; йо < 10; йо+=1){
    ежели(йо%2) тогда{
        вещать(йо)
    }
};

вещать("Конецъ!")